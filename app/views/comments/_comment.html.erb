<div class="form-control">
  <%= comment.user.email%> posted
  <%= simple_format comment.body %>

  <%= link_to "Reply", "#" %>
  <%= render partial: 'comments/form', locals: { commentable: @recipe,parent_id:comment.id, class:"d-none"} %>
</div>